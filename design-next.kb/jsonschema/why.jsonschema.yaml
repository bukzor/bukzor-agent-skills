$schema: "http://json-schema.org/draft-07/schema#"
description: |
  Motivation edges: file-relative path references to the entries this
  one exists to serve — normally the layer above; the collection's
  CLAUDE.md says which.
type: array
minItems: 1
items:
  type: string
