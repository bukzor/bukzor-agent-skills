---
why:
  - token-economy
  - mechanism-over-exhortation
---

# Judgment Triggers Remain Scannable

Triggers whose condition is a mental-state predicate — "when
evaluating a contested position", "when instructions are
inconsistent" — cannot compile to a deterministic-enforcement adapter
(see `../040-design.kb/delivery-contract.md`). These remain a
filename-indexed bank: cheap to scan during planning, bodies unloaded
until a trigger fires.

Checkable: the residual bank contains *only* judgment-shaped
triggers; any entry naming a detectable action is misfiled (it
belongs to an adapter-enforced trigger, per
`action-triggers-enforced-deterministically.md`). This keeps the scan
tax proportional to the genuinely undetectable cases.
