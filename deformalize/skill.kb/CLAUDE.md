# skill.kb -- maintenance guide

Maintainer-side elaborations of the manual. `must-read.kb/` holds the
one trigger this skill currently needs: what to do when the code
escalation is actually wanted, kept out of `SKILL.md` so a run that
never needs it never loads it.
