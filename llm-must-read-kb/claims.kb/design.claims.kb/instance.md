---
label: INSTANCE
standing: agent
why:
  - policy.md
ontology:
  - making-code-changes
  - stripe
stale-when: the bank at ~/.claude/must-read.kb no longer has the entries named here -- the proper nouns are the first thing to throw away
---

# Instance -- today's bank, by name

Coins two proper nouns: `making-code-changes`, the one occasion in
`~/.claude/must-read.kb` that meets the gate rule and the threshold
today; and `stripe`, the one trigger whose occasion is a project's.
Every other trigger in the bank stays where it is: the discourse
triggers are universal, the git and tool prefixes fire near every
session, and the environment triggers are one file each.
