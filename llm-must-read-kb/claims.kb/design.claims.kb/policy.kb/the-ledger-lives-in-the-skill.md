---
label: LEDGER_HOME
standing: user
authority:
  address: ba648ef5
  words: "I think i'd rather have a new, near-empty design-claims kb than to use ADRs. I've (nearly?) entirely abandoned the ADRs in favor of design-claims."
  about: where this design's claims are filed
---

# The Ledger Lives in the Skill

This design's claims file to `llm-must-read-kb/claims.kb/design.claims.kb/`,
shaped like `llm-kb/claims.kb/`; the plain-directory claim files to
llm-kb's own ledger, since it binds llm-kb's base contract. ADRs are
not used for this. `Skill(llm-triggers)` absorbs the ledger at the
fold.

> [!@bukzor] ba648ef5
> I think i'd rather have a new, near-empty design-claims kb than to
> use ADRs. I've (nearly?) entirely abandoned the ADRs in favor of
> design-claims.
