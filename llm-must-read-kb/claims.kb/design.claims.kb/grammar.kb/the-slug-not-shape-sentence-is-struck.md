---
label: SLUG_LINE
standing: user
todo: true
why:
  - a-set-has-the-bank-shape-at-every-depth.md
authority:
  address: ba648ef5
  words: "is _already_ wrong, no? after/ and before/ are meant to be load-bearing parts of the \"categorization\". Anyhow, agreed; it needs to go."
  about: the SKILL.md sentence "the trigger slug, not the directory shape, carries the categorization"
---

# The "Slug, Not Shape" Sentence Is Struck

`../../../SKILL.md` says, under Nesting, that "the trigger slug, not the
directory shape, carries the categorization". Junctures are
directories, so the sentence was false before sets existed and is
doubly so after: the path is the trigger phrase, and directory shape is
half of it. The sentence is removed and the Nesting section documents
`while/` in its place.

> [!@bukzor] ba648ef5
> is _already_ wrong, no? after/ and before/ are meant to be
> load-bearing parts of the "categorization". Anyhow, agreed; it needs
> to go.
