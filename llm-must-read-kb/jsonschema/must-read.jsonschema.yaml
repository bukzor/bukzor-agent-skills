# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
title: must-read.kb entry frontmatter
description: |
  A bank entry's filename is its trigger and its body is the directive;
  frontmatter carries only what the entry reads onward.

type: object
additionalProperties: false
properties:
  triggers:
    $ref: "skill://llm-kb/jsonschema/triggers.jsonschema.yaml"
