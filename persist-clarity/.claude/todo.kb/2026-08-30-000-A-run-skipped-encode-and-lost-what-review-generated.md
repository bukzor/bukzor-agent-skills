---
managed-by: Skill(llm-subtask)
status: done
closeout: |
  Read and ruled 2026-08-31. The mispriced render is answered in the
  encode beat (MANDATE accepted: "the in-chat ledger mandatory,
  yes"). The closing-condition repair was rejected — persist is now
  user-elected ("the operation can end at chat-ledger"), so a late
  mint belongs to the still-mandatory chat ledger and nothing
  auto-lands on disk.
---

# A run skipped encode and lost what review generated

**Priority:** bears directly on ENCOURAGE? in
`./2026-08-29-000-Rule-on-the-redesign-open-questions.md`, so it wants
reading before that sitting
**Complexity:** the record is below; no repair is proposed here
**Context:** a `/persist-clarity` run on 2026-08-30, against the
three-beat text committed the day before

## What Happened

The run was invoked with arguments that split the operation: persist
what the session was certain of, report the residue, then think about
what policies that clarity suggested.

The agent skipped the encode beat. It filed two certain findings into
homes that already existed, committed and pushed them, then wrote the
residue report, then generated three policy proposals and one lens in
prose.

None of the four entered a ledger. The persist beat had already
closed, so nothing carried them anywhere, and they existed only in the
response until the owner asked where they were. The lens was the
session's strongest product: that a rule written into the artifact the
next agent must edit anyway is honored without being taught — evidenced
by two propagation events with no contact between their authors.

## Two Things Made It Possible

**The encode beat was declined on a cost it does not have.** The agent
priced the render as though it minted a file — a second home beside
the project's real ledgers — and refused it as over-ceremony for six
claims. Encode is a render in the response. It creates nothing.

**Verification and the closing condition both sit inside persist.**
"Follow your own re-entry path reading only what's on disk" ran, and
ran honestly, against the state as it stood before the last generative
act. Review generates: asking a reader what a body of work implies is
itself a synthesis act, and it reliably produces lenses. The stated
end — "at a commit, not before" — was met while the claims made after
that commit had nowhere to be.

## Success Criteria

- [x] The owner has read this and said whether it changes anything —
      it did; see closeout

## Address

Session `6c04b3e5-d49c-46e0-a5cb-d8547a4ff47d`, project
`-home-bukzor-claude-meta-reasoning`. The owner's question that
surfaced it: "where/how are those policies persisted? specifically the
'meta-finding'".

The two propagation events are in `private.meta-reasoning-corpus`:
`repo-health.sh`'s extent rule reaching `lib/verify-ownership.py`'s
docstring, and the permanently-red doctrine reaching devlog
`2026-08-29-001`.
