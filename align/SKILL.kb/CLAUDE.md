# SKILL.kb -- maintenance guide

Procedures live at this level, one per file; a trigger in
`must-read.kb/` (`Skill(llm-must-read-kb)`) names each one's
occasion and points at it by path. Trigger says when; procedure says
how.
