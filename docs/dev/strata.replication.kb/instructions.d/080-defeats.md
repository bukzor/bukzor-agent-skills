Now critique the ledger on its own terms.

Its filing rule: a claim belongs in the earliest theory whose ontology -- its own plus its priors' -- admits every word the claim needs. So:

- any claim mis-filed by that rule?
- any ontology widened with machinery that isn't really the theory's own, where an auxiliary prior theory was the right move?
- any claim standing `bare` that's actually a judgment? any `agent`-signed claim that's really a definition, needing no signature?
- missing or unfalsifiable `defeated by:` lines? mutual-support rings in the `why:` graph?
- is OBLIGATION the right open claim, and the only one?
- does question.md's table still match question.kb/?

Then the standing review lenses, in rank order (~/.claude/must-read.kb/when/asked-to-review-work.md) -- alignment, effectiveness, simplicity; accuracy-auditing isn't the deliverable. Press hardest on simplicity: twelve theories over one skill ecosystem is a lot of apparatus, and the ledger claims to *be* the simplification. Would less serve the same purpose?

Deliver: proposed defeats, one per line, each naming the claim file and the observation that defeats it. Then the bottom line -- which account is better where, and the single highest-value change to the ledger. Propose the diffs; don't write them.
