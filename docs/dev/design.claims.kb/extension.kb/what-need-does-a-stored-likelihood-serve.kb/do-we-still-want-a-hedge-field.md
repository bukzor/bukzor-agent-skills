---
label: HEDGE_KEEP
standing: open
why:
  - ../what-need-does-a-stored-likelihood-serve.md
---

# Do We Still Want a Hedge Field At All?

"do we want this thing, at all, now, going forward?" (2026-08-16).

The need is granted; what is open is whether a stored field is how to
meet it. Two things bear on the answer, neither settled:

- `standing:` already carries part of the load. `open` is a hedge,
  and a claim the agent declines to sign is one it is unsure of --
  what a scalar adds over that is degree, on claims it would sign.
- ~62 non-default values are live across six sites, concentrated in
  `summer-programming-project/2026/.plan` (34) and
  `template.python-project` (17). Dropping the field is not free.
