---
label: SUCCESSOR
standing: bare
why:
  - where-the-migration-guide-and-its-trigger-live.md
  - migration-plan.md
---

# What the Successor Is Called

`llm-discourse-graph`, in the same place -- there is no separate
successor to name. The skill's basis changes; the skill does not get
replaced, and GUIDE_HOME puts the trigger and the guide inside it, so
the name has to survive or nothing routes.

> [!@bukzor]
>
> I'm pretty sure it will be named "llm-discourse-graph", sitting exactly where the old thing set.
> In fact that's required if we want the migration story we've already set up to work right:
> there's both a must-read trigger and a migraion guide in the skill that describes what to do if you encounter the old format.

Standing is `bare` on the strength of that second sentence: it is a
derivation from the migration plan, not a preference, so it needs no
signature.

The declined argument: "discourse graph" names the five-type ontology
that is going away, so the name should go with it. It loses to
routing -- and the name denotes the subject matter, which the
extension keeps.
