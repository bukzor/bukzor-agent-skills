[project]
name = "engine-tower"
version = "0.1.0"
description = "Mechanized witnesses for the engine-tower claim ledger (docs/dev/strata.ledger.md)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
