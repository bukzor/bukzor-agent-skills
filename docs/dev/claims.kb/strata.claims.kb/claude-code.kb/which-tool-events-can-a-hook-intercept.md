---
label: CC_HOOKS
standing: open
why:
  - ../carriage.kb/hooks-reach-observable-junctures-prose-reaches-semantic-ones.md
---

# Which tool events can a hook intercept, and can it read the pending file?

An answer settles how many semantic junctures have an observable proxy at
all. Check: the Claude Code hooks reference, then one PreToolUse hook that
prints its input on a Write into a `.kb` directory.
