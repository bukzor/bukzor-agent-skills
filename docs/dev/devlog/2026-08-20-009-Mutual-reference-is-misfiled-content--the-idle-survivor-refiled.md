# Mutual reference is misfiled content; the idle survivor refiled

Session: onto-review (continuing 008).

## The ruling

Defending the SCALAR <- FATE keep, I argued FATE's backticked
file-cite of SCALAR was "not a mention" because the mentions lint
exempts backticks. User overruled: a file-path cite is a reference,
backticked or not -- the lint's exemptions bound what a scan may
auto-flag, not what reading must count. So FATE and SCALAR mutually
referred (arrow one way, prose the other), which is a
separation-of-concerns smell; user proposed merge or nesting as
candidate repairs.

## The anatomy (and why not merge)

Reading all three parties found every crossing reference belonged
to content sitting in the wrong file:

- FATE's citing section ("where the stored-likelihood argument
  belongs") was a lodger FATE's own text rules off-topic ("not on
  this question") -- and a restatement of an existing strata claim,
  view.kb's a-written-projection-is-cache-or-testimony.md
  (TESTIMONY). Discovered via label-collision check while about to
  mint the same taxonomy again.
- SCALAR's only FATE-dependent sentence duplicated migration-plan's
  commitment #4 verbatim, down to the same user quote ("We just
  keep the field until it's closed").

The FATE/SCALAR boundary itself was sound: past-facing closed
ruling vs future-facing open need, different standings, different
lifecycles. Merge would have fused their .kbs (rejected candidates
+ open hedge questions) and buried a live need under a settled
disposition; either nesting inverts scope one way or the other.
Extraction dissolved the mutual reference without moving either
claim.

## The refiling (user-ratified)

- New extension.kb/the-stored-likelihood-is-neither-cache-nor-
  testimony.md (LIKELIHOOD, bare): the one-line application of
  TESTIMONY to `likelihood`, citing it by why:; git closes the gap,
  so a carried-over value is lawful testimony unrewritten.
- FATE: lodger section replaced by a one-line evidence cite; why:
  += LIKELIHOOD. References no sibling anymore.
- SCALAR: tail now "migration-plan.md holds the field in escrow";
  why: retargeted FATE -> migration-plan.md (a plain claim, no .kb
  -- not an import, so no census entry).
- MIGRATE: why: += LIKELIHOOD (its verbatim-carry commitment's
  lawfulness rests on it).

Scans after: SCALAR <- FATE gone -- dissolved by repair, never
adjudicated keep. Mentions 0; graph acyclic. The queue's one entry
is new, minted by a concurrent session's in-flight notation.kb
work: left for its owner. Pre-existing orphans noticed in passing,
not touched: authorship.kb's instances-cite-the-skill and
skills-are-operator-composed never joined the graph.

## Procedures touched up

- IDLE_TEST: the resolution menu gains the middle outcome --
  support real but misfiled (duplicate of a ruling homed elsewhere,
  or a lodger the prior rules off-topic); refile and the arrow
  dissolves. The tell is mutual reach; a file-path cite is a
  reference, backticked or not.
- IDLE_UNDECIDABLE: verdicts are keep, cull, or refile; figure
  refreshed (1 idle of 53, queue turning over with new imports).

## Convention

Before minting a claim, grep the fleet's labels for its key words:
the collision check that found TESTIMONY is what turned an
extraction into a dedup. And when reading an idle import, look for
the prior's prose reaching back into the taker -- mutual reach
means misfiled content, and refiling beats both keeping and
merging.
