# Question theory; act-review fallout; single-sigil seam

Session: "formalization" (the strata-ledger arc, continued).

## What happened

- Added `question` as the ledger's historical theory (188436b): the
  eight driving design problems, one claim each, as wrestled / as
  well-posed / decomposed-by / residue. Sits above `fleet` --
  historically the seed, logically the capstone. `question.md` is the
  stamped synthesis. Side discovery: loose `.md` files in a `*.kb/`
  collection validate against the parent's schema, so
  `strata.ledger.jsonschema.yaml` now exists and says what those
  files are (stamped theory syntheses).
- A sibling session landed the purpose restructure (32b1a76: `purpose`
  root, act-review rulings, 12 new claims). Verified it independently;
  question rode through untouched. Refreshed question.kb afterward:
  SEMANTICS now cites STANCE (the dissolution deepened -- the chooser
  of a defeat semantics is a stance, no global arbiter even in
  principle), STALENESS cites FRESH_COST, RETENTION cites FLOOR.

## Conventions established

- `question.kb/` may hold proper nouns: they are past-tense
  provenance, which `fleet`'s defeater (named systems changing)
  cannot reach. Exception recorded in `strata.ledger.kb/CLAUDE.md`.
- Cross-session etiquette that worked: verify a sibling's landing
  independently (validators + suite) before building on it; never
  commit another session's uncommitted work unvouched.

## Open items (persisted, not here)

`.claude/todo.md`: the single-sigil seam (STANCE vs one-place
`standing:` frontmatter -- goes live when two assessors first
disagree on one claim) and label prefix-freedom enforcement.
`.claude/todo.kb/2026-08-09-000-engine-tower-incubator-follow-ups.md`:
the live operator rulings (Lean procurement, uv.lock, TWICE shape)
and unclaimed offers. `llm-kb/.claude/todo.md`: question.md as field
case 2 for the synthesis-freshness check (date-grain stamps miss
same-day drift).

## Unresolved

The user referred to "mandate.kb"; no file by that name exists
anywhere searched (this repo, sibling repos, ~/.claude). Read
`purpose.kb` -- "the user's stipulations" -- as the referent. If a
literal mandate.kb was meant, it never reached this filesystem.
