# Devlog: 2026-08-20 — Docket cleared; the adjudication procedure is now claims

## Focus

Execute the remainder of the ownership docket on the user's
"continue", then file the decision procedure the adjudication ran on
into ownership.kb as claims — the second half of what devlog 003 left
"offered, not taken."

## Rulings executed (commit 0e18b77)

- Decision 2 — 'ontology' (stance × notation): notation keeps;
  stance culled its entry, and PARSIMONY — the one stance.kb claim
  saying the word — was retitled 'Prefer Fewer Distinctions' and
  reworded into stance's own vocabulary ("paid for by every author,
  at every act"). File renamed via git mv to
  `prefer-fewer-distinctions.md`; the three notation.kb citers swept.
- Decision 3 — 'retraction' (history × standing): history owns — the
  open RETRACTION claim is history's own debt; standing culled.
  Standing's interior keeps saying the word under transitive reach
  (standing→reference→record→history).
- Decision 4 — 'transport' (record × data-representation): record
  owns the general law (MIGRATE); data-representation culled and now
  imports record directly — its ROUNDTRIP is an instance, so the new
  arrow carries real support and no cycle.
- Decision 5 — setting→run (idle, formalize): retargeted to
  `purpose.md`. A plain cull would have stranded setting's 'basis'
  (purpose's word, previously licensed only through run) — the
  fleet's live nonmonotone case. The retargeted arrow scans live.
- Idle keeps on witness, no edit: SCALAR→FATE (vacuous — the cited
  theory stipulates no ontology), IDENTIFICATION→purpose,
  RUN→identification.

## The procedure, filed (this commit)

Four claims in ownership.kb:

- **DOCKET** (bare) — a sibling double is docketed at its
  stipulations; with no owner, the repair menu lifts to
  cull / narrow / shared prior, and the speech-level positions wait.
- **PLAIN** (user) — the two-move menu for a plain-English double,
  carrying the user's ruling verbatim as `authority:`.
- **TRIAGE** (bare) — pending speech sorts by candidate owner:
  licensed under every / none / some resolutions; computable, so a
  scan feature in waiting.
- **IDLE_TEST** (agent) — an idle import resolves on the support
  lens alone; culls check for stranded words first; retarget when
  the support points elsewhere; vacuous idleness is a scan artifact.

CHECK_OWNERSHIP's agentic arm now cites the four; ownership.kb's
CLAUDE.md admits procedure commitments; SHOULD_OWN trims to what is
genuinely still open — the middle band, neither plainly common nor
plainly a coinage — and cites PLAIN for the settled end.

## Verification

- Census: 0 sibling doubles (13 at first scan this morning), 6
  inside, 48 foreign; 3 idle of 53 imports, all keeps on witness.
  The new claims minted nothing.
- Flatten clean; graph 88 nodes / 84 edges, acyclic; mentions 0
  unimported; schema 33/33; stipulations 307 fleet-wide (five
  ledgers, deformalize included).

## Work items surfaced, not taken

- TRIAGE's three-way partition as a scan feature.
- Auto-marking vacuously idle imports (cited theory with empty
  ontology) in the idle queue's output.

## References

- Commit 0e18b77 — the rulings; this commit — the procedure claims.
- devlog 2026-08-20-003 — the plain-English half of the docket and
  the ruling this session continued under.
