# TODO

- [ ] USER: re-paste the SKILL.md Core block into claude.ai preferences
      (verbatim-shared; changed 2026-07-24: sigils, governance line,
      open-claims policy; changed 2026-07-28: the three example claims
      now carry the standings they are cited with, `XY` -> `AR`,
      Policy 1 folded into `LC`)
- [ ] discharge SG: verify the sigil set against the derivation chat once
      captured/exported — DM's rejected `'` sigil suggests there may
      have been a fuller set (e.g. superseded/retracted). The Core
      block's `SG?` records exactly this debt; do not "correct" it to
      `SG!` before the check lands
