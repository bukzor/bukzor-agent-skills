# self-audit.kb -- maintenance guide

One audit per file: a way a claims.kb rots, the scan that catches
it, and what a finding means. Audits a plain `.kb` also needs belong
to `Skill(llm-kb)` -- cite, don't copy, or the audit acquires two
versions that drift.
