$comment: |
  NO-REF ruled 2026-08-21 (2026-05-15-000-schema-propagation-from-canonical):
  deliberately NOT a $ref stub onto
  skill://incident-forensics/jsonschema/timeline.jsonschema.yaml: the
  category name collides, the meaning doesn't -- that canonical is an
  incident chronology, this is a party's running order. Reuse follows
  meaning, not spelling; where reuse is real this example takes it
  (status/budget, via jsonschema/).
$schema: "http://json-schema.org/draft-07/schema#"
description: Schema for timeline phases in timeline.kb/
type: object
additionalProperties: false
required:
  - time
properties:
  time:
    description: "Time block for this phase, e.g., '2:00 PM - 2:30 PM'"
    type: string
